<Window x:Class="PadelSimple.Desktop.Views.LoginWindow"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
Title="PadelSimple – Login" Width="380" Height="240">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Aanmelden" FontSize="20" FontWeight="Bold"/>
        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,12,0,0">
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>
            <PasswordBox x:Name="Pwd" Margin="4"/>
        </StackPanel>
        <Button Grid.Row="2" Content="Login" Width="120" HorizontalAlignment="Left" Margin="0,12,0,0"
Click="Button_Click"/>
    </Grid>
</Window>